# build output
dist/
.vercel/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables — se ignoran TODOS los .env locales (incluye
# .env.local, .env.development, .env.staging, .env.production y sus *.local),
# salvo .env.example que sí se versiona como plantilla.
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

.agents/
skills-lock.json
.claude/
.codex/
.impeccable/

# caché de registro de skills (autogenerado, no se versiona)
.atl/

# material de referencia de diseño: mockups, fotos de producto y capturas que se
# miran mientras se construye, pero que NO son fuente del sitio. Son 24 MB de
# binarios que nadie importa desde `src/` (un solo PNG pesa 8 MB): infla el clone
# para siempre y basta un `git add -A` distraído para que entren.
design-reference/
