# URL base del backend (lo consume el BFF server-side). No es secreto.
BACKEND_URL="http://localhost:8080"
# Secreto para firmar la cookie de sesión admin. NO commitear el valor real.
SESSION_SECRET=""